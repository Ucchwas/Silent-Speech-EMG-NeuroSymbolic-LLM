torch
transformers>=4.44
numpy
scipy
tqdm
unidecode
sentencepiece
protobuf

# Optional: only needed to re-extract features from raw EMG recordings
librosa
soundfile
praat-textgrids

# Optional: spreadsheet export / external WER cross-check.
# WER, CER and the bootstrap CIs are computed in scripts/metrics.py, so
# neither package is required to reproduce the paper's tables.
pandas
openpyxl
jiwer
